name: Deploy Workspace CORS Hotfix

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  information:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deployment prerequisite
        run: |
          echo "The CORS source hotfix is stored on branch backend."
          echo "Production deployment requires the full Backend source or an existing GHCR backend image."
          echo "Target origin: https://workspace.app.squaredgroup.studio"
